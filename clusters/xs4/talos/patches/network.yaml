---
# TODO: Generate with Ansible or bash ?
# Set interfaces to DHCP with VIP
machine:
  network:
    interfaces:
      - deviceSelector:
          physical: true
        dhcp: true
    nameservers:
      - 192.168.81.200
  time:
    servers:
      - 0.nl.pool.ntp.org
    